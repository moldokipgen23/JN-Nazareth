{
  "$schema": "https://schema.railpack.com",
  "provider": "php",
  "packages": {
    "php": "8.4",
    "node": "20"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan db:seed --class=DatabaseSeeder --force && php artisan storage:link --force && php artisan config:cache && php artisan route:cache && php artisan view:cache && php -S 0.0.0.0:$PORT -t public"
  }
}
